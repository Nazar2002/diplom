enum AuthEnum {
  SIGN_UP = '/auth/sign-up',

  SIGN_IN = '/auth/sign-in',

  AUTHENTICATE = '/auth/authenticate'
}

enum BasketEnum {
  BASKET = '/basket'
}

enum BrandTypeEnum {
  BRAND = '/brand',

  TYPE = '/type'
}

enum DeviceEnum {
  DEVICE = '/device/item',

  DEVICE_LIST = '/device/items'
}

export { AuthEnum, BasketEnum, BrandTypeEnum, DeviceEnum };
